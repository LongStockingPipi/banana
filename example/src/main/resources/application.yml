# server config
server:
  port: 8090

# log config
logging:
  file: banana.log

#spring config
spring:
  thymeleaf:
    cache: false

#banana security config
security:
  requestUriWithAjax: /api
  loginPage: /page/login
  welcomePage: /page/index
  authRequestUri: /api/auth
